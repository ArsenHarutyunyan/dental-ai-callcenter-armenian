# Copy to .env and fill in. Nothing here is required to be secret in a purely
# local/offline setup, but keep .env out of git regardless.

SECRET_KEY=change-me
OLLAMA_URL=http://localhost:11434/api/chat
OLLAMA_MODEL=qwen3:4b
